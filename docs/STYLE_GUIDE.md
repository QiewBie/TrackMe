# TrackMe Design System & Style Guide (v2.0)

> **Core Philosophy**: "Systematic, Semantic, & Premium."
> Ми не стилізуємо "на око". Ми використовуємо попередньо визначені токени.

---

## 1. 🎨 Кольорова Система (Color System)

Всі кольори визначені семантично в `index.css` і адаптуються до теми.

### 1.1. Структура Фону (Background Levels)
| Рівень | Токен | Light (Slate) | Dark (Slate) | Призначення |
| :--- | :--- | :--- | :--- | :--- |
| **L0** (Canvas) | `bg-bg-main` | `#f8fafc` | `#020617` | Глобальний фон сторінки (`<body>`, `<Page>`). Найтемніший/Найсвітліший. |
| **L1** (Surface) | `bg-bg-surface` | `#ffffff` | `#0f172a` | Картки, Сайдбари, Модальні вікна. Основний контейнер контенту. |
| **L2** (Subtle) | `bg-bg-subtle` | `#f1f5f9` | `#1e293b` | Вторинні блоки, неактивні кнопки, ховер-стани списків. |
| **L3** (Inverse) | `bg-bg-inverse` | `#020617` | `#f8fafc` | Тултіпи, Тости, Контрастні елементи. |
| **L4** (Brand Text) | `text-on-brand` | `#ffffff` | `#ffffff` | Текст на кнопках бренду (авто-адаптація для світлих тем). |

### 1.2. Акцентний Колір (Brand Primary)
Базовий колір: **Blue-600** (`#2563eb`) у Light, **Blue-500** (`#3b82f6`) у Dark.
*   **Використання**: `bg-brand-primary`, `text-brand-primary`.
*   **Тіні**: `shadow-brand/20` (легка), `shadow-brand/40` (активна).
*   **Правило**: Ніколи не використовуйте `bg-blue-600` напряму. Тільки `bg-brand-primary`.

### 1.3. Спеціальні Фони (Special States)
*   **Overlay**: `bg-overlay` (аліас для `bg-black/60`). Використовувати *тільки* для бекдропів модальних вікон.
*   **Solid Overlay**: `bg-bg-main` + `z-[9999]`. Для повноекранних станів (наприклад, "Session Complete"), коли треба приховати весь інтерфейс.

### 1.4. Реєстр Тем (Available Themes)
Теми визначаються в `src/config/ThemeRegistry.ts`.
1.  **Default (Blue)**: Стандартна, корпоративна.
2.  **Serenity (Purple)**: Спокійна, творча.
3.  **Amber**: Тепла, енергійна.
4.  **Rose**: М'яка, вечірня.
5.  **Sage (Birch)**: Природна, низький контраст ("Restrained").
6.  **Monochrome**: Високий контраст, мінімалізм.

### 1.5. Градієнти (Gradients)
*   `bg-brand-gradient`: Використовуйте для кнопок-акцентів або хедерів.
*   Змінні: `var(--gradient-start)` -> `var(--gradient-mid)` -> `var(--gradient-end)`.

### 1.6. Status Colors (Семантичні Стани)
*   **Success**: `text-status-success` (Green). Виконані завдання.
*   **Error**: `text-status-error` (Red). Помилки, деструктивні дії.
*   **Warning**: `text-status-warning` (Amber). Попередження.
*   **Info**: `text-status-info` (Blue). Підказки.

### 1.7. Tag Colors (Мітки)
Для категорій ми використовуємо набір `tag-*` (blue, red, green, amber, purple, pink, cyan, indigo, slate, emerald). Вони завжди використовуються як `bg-tag-RED` або `text-tag-RED`.

---

---

## 2. 🛠️ Технічна Реалізація (Under the Hood)

Ця секція описує, як система працює на рівні коду.

### 2.1. Theme Registry (`ThemeRegistry.ts`)
Ми не хардкодимо кольори в CSS. Всі теми живуть у `src/config/ThemeRegistry.ts`.
Кожна тема визначає палітру HSL значень:
```typescript
{
  id: 'midnight',
  colors: {
    light: {
      '--bg-main': '220 14% 96%', // Slate-50
      '--brand-primary': '221 83% 53%', // Blue-600
    },
    dark: {
      '--bg-main': '222 47% 11%', // Slate-950
      // ...
    }
  }
}
```

### 2.2. Runtime Injection
Компонент `ThemeProvider` (`src/context/ThemeContext.tsx`) відстежує зміну теми та **синхронно** (`useLayoutEffect`) записує CSS змінні в `:root` (`document.documentElement`).
Це дозволяє змінювати тему без перезавантаження сторінки і без "блимання" (FOUC).

### 2.3. Tailwind Mapping
Tailwind налаштований (`tailwind.config.js`) використовувати ці змінні через `hsl()` обгортку:
```javascript
colors: {
  bg: {
    main: 'hsl(var(--bg-main) / <alpha-value>)',
  }
}
```
Це дозволяє писати `bg-bg-main/50` і отримувати напівпрозорий фон, який коректно адаптується під будь-яку тему.

---

## 3. ✍️ Типографія (Typography)

Шрифт: **Manrope** (Sans-serif).

### 2.1. Ієрархія (Hierarchy)
| Елемент | Класи | Розмір | Вага | Трекінг | Призначення |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **Display** | `text-5xl font-black tracking-tight` | 48px | 900 | -0.025em | Екрани успіху, великі таймери. |
| **H1** | `text-4xl font-extrabold tracking-tight` | 36px | 800 | -0.025em | Заголовки сторінок. |
| **H2** | `text-2xl font-bold` | 24px | 700 | Normal | Заголовки карток, секцій. |
| **H3** | `text-lg font-bold` | 18px | 700 | Normal | Підзаголовки. |
| **Body Prominent** | `text-base font-medium` | 16px | 500 | Normal | Основний акцентний текст. |
| **Body Regular** | `text-sm font-normal` | 14px | 400 | Normal | Звичайний опис, параграфи. |
| **Caption/Label** | `text-xs font-bold uppercase tracking-wider` | 12px | 700 | +0.05em | Підписи полів, бейджі, мета-дані. |

### 2.2. Колір Тексту
*   `text-text-primary`: Заголовки, інпути, кнопки. Контраст 100%.
*   `text-text-secondary`: Описи, підказки, іконки в кнопках `ghost`. Контраст ~70%.
*   `text-ui-disabled`: Плейсхолдери, заблоковані елементи. Контраст ~40%.

---

## 3. 🧱 Компоненти Інтерфейсу (Components)

### 3.1. Кнопки (Buttons)
Стандартна висота: **40px (`h-10`)** або **56px (`h-14`)** для великих дій (CTA).
*   **Radius**: `rounded-xl` (стандарт), `rounded-2xl` (великі кнопки).
*   **Primary**: `bg-brand-primary text-white shadow-lg shadow-brand/20 hover:shadow-brand/40`.
*   **Ghost**: `text-text-secondary hover:text-text-primary hover:bg-bg-subtle`.
*   **Icon-Only**: Використовувати аспект `aspect-square` або клас `w-10 h-10 p-0`.

### 3.2. Поля Вводу (Inputs)
Всі інпути (`text`, `number`, `password`) мають ідентичний стиль.
*   **Фон**: `bg-bg-surface` (Строго! Ніколи `bg-bg-subtle`).
*   **Бордер**: `border border-border`.
*   **Фокус**: `ring-2 ring-brand-primary/20 border-brand-primary`.
*   **Висота**: `h-10` (40px) стандарт.
*   **Placeholder**: `placeholder:text-ui-disabled`.

### 3.3. Чекбокси та Тоггли (Toggles)
*   **Toggle Track**: `w-11 h-6 rounded-full`.
    *   ON: `bg-brand-primary`.
    *   OFF: `bg-bg-subtle` (Light) / `bg-bg-surface` (Dark).
    *   **Border**: Обов'язково `border border-transparent` (ON) або `border-border-subtle` (OFF) для контрасту в світлій темі.
*   **Checkboxes**: `w-5 h-5 rounded border`.
    *   Checked: `bg-brand-primary border-brand-primary text-white`.

### 3.4. Картки (Cards)
Використовується для списків завдань, плейлистів, віджетів.
*   **Container**: `bg-bg-surface border border-border rounded-2xl`.
*   **Hover**: `hover:border-brand-primary/50 transition-colors`.
*   **Shadow**: `shadow-sm` (стандарт), `shadow-md` (ховер).

---

## 4. 📐 Макет та Навігація (Layout & Nav)

### 4.1. Сайдбар (Sidebar)
*   **Ширина**: Фіксована. Десктоп – **280px** (або `w-64`).
*   **Фон**: `bg-bg-main` (зливається з фоном сторінки) або `bg-bg-surface` (виділений).
*   **Мобільна версія**:
    *   Поведінка: Overlay (виїжджає поверх контенту).
    *   Z-Index: `z-40` (під модалками, але над контентом).
    *   Backdrop: `bg-overlay`.

### 4.2. Модальні Вікна (Modals)
*   **Z-Index**: `z-50`.
*   **Центрування**: Flex-контейнер `items-center justify-center`.
*   **Відступ**: `p-4` (мобільний), `p-6` (десктоп).
*   **Анімація**: `animate-pop-in` (Scale 0.95 -> 1.0, Opacity 0 -> 1).

### 4.3. Z-Index Stack (Шари)
Всі Z-Index значення визначені в `tailwind.config.js` як семантичні токени. Не використовуйте "магічні числа".

1.  `z-0`: Фон.
2.  `z-10`: Контент сторінки.
3.  `z-fixed (40)`: Sidebar, Sticky Headers.
4.  `z-overlay (60)`: Затемнення фону (Backdrops).
5.  `z-modal (70)`: Модальні вікна.
6.  `z-popover (80)`: Випадаючі списки, Пікери дат.
7.  `z-toast (90)`: Повідомлення, Тости.
8.  `z-[100+]`: Критичні системні оверлеї (наприклад, Session Complete).

---

## 5. ✨ Ефекти та Анімації

### 5.1. Тіні (Shadows)
Ми використовуємо кольорові тіні для створення ефекту "Glow".
*   `shadow-brand/20`: Дефолтна тінь для кнопок бренду.
*   `shadow-brand-deep`: Глибока тінь для активних карток/тасків.
*   `shadow-brand-card`: Тінь для карток у фокусі.
*   `shadow-status-error/20`: Тінь для кнопок небезпеки.

### 5.2. Анімації (Animations)
Ми використовуємо кастомні анімації в `tailwind.config.js`:

| Клас | Опис | Призначення |
| :--- | :--- | :--- |
| `animate-pop-in` | Scale 0.95 -> 1.0, Opacity 0 -> 1 | Модальні вікна, поп-овери. |
| `animate-slide-in` | TranslateY -15px -> 0, Opacity 0 -> 1 | Тости, нотифікації. |
| `animate-check-bounce` | Scale 1 -> 0.8 -> 1.1 | Чекбокси при натисканні. |

### 5.3. Transition Timing
*   **Standard**: `ease-in-out` (300ms).
*   **Spring**: `cubic-bezier(0.16, 1, 0.3, 1)` (використовується для модалок, відчувається як iOS).

---

## 6. 📝 Rich Text & ProseMirror
Для редактора нотаток ми стилізуємо клас `.ProseMirror` глобально (`index.css`):
*   **H1**: 1.8em, Bold (800).
*   **H2**: 1.4em, Bold (700).
*   **Lists**: `ul` (disc), `ol` (decimal) з відступом `1.5em`.
*   **Blockquote**: Бордер зліва (`border-l-3`), курсив.

## 7. 📏 Spacing & Sizing Rules
*   **Sidebar Width**: `w-sidebar-focus` (400px).
*   **Card Padding**: `p-card` (1.5rem / 24px).
*   **Input Spacing**: `p-input` (0.75rem / 12px).
*   **Scrollbars**: Кастомні, тонкі (6px), `rounded-full`.
